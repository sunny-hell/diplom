function drawSubplot(x, y)
    set(gca, 'xlim',
    plot(x, y);
    yPos = 0.3;
    hold on
    plot(get(gca,'xlim'), [yPos yPos], 'r'); % Adapts to x limits of current axes
    hold off
    ylabel('Q_k');
end